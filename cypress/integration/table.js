/// <reference types="cypress"/>
const _ = Cypress._
import { expectationTable } from '../../src/expectation'
import { expectTableContent } from "../../src/table"


describe('expectTableContent', () => {
 it('can validate based on the values in expectationTable', () => {

 })
})

